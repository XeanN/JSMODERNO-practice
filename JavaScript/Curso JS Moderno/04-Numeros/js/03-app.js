let resultado;

//PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.8);  
resultado = Math.round(2.2);
resultado = Math.round(2.6);
resultado = Math.round(2.5);
resultado = Math.round(2.4);

// Redondear hacia arriba
resultado = Math.ceil(2.1);

// Redondear hacia abajo
resultado = Math.floor(2.9);

// Raíz Cuadrada
resultado = Math.sqrt(144);

//Absoluto
resultado = Math.abs(-550);

//Potencia
resultado = Math.pow(5, 3);

// Mínimo
resultado = Math.min(3, 5, 1, 12, -3, -100);

// Máximo
resultado = Math.max(3, 5, 1, 12, -3, -100);

// Aleatorio
resultado = Math.random();

//Aleatorio dentro de un rango...  Por ejemplo Rango 30
resultado = Math.floor( Math.random() * 30);

console.log(resultado);